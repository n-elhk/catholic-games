# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build the-verb-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t the-verb-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system the-verb-api && \
          adduser --system -G the-verb-api the-verb-api

COPY dist/apps/the-verb-api the-verb-api
RUN chown -R the-verb-api:the-verb-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix the-verb-api --omit=dev -f install

# Expose port 3000 (default port)
EXPOSE 3000

CMD [ "node", "the-verb-api" ]
